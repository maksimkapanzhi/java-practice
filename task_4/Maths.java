package hw_3.task_1.task_4;

import static java.lang.Math.cos;

public class Maths {
    public static void main(String[] args) {
       double a = (((( (cos(180)) + (256 / 8) - (Math.pow(2, 3) ))* Math.PI)));
        System.out.println(a);// в градусы не переводил, поскольку в задании не указано, что такое 180 - градусы или радианы
    }
}
